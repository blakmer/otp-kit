```js
import Logo from '../Logo'
;<NavPanel
  header={<Logo />}
  onSearch={(e) => {}}
  onHelp={() => {}}
  actions={[
    { icon: 'profile', action: () => {} },
    { icon: 'close', action: () => {} },
  ]}
/>
```
